[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    contract: hits a real, running API instance (see tests/contract/README.md) rather than stubbed requests
