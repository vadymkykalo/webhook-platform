# ============================================================================
# Hookflow - Environment Configuration Reference
# ============================================================================
# This is the catalogue of every setting, for looking things up. It is NOT the
# way to install: the secrets in here are public, because this file is in a
# public repository, and copying it verbatim ships a deployment whose
# encryption key and JWT secret anyone can read.
#
#   curl -fsSL https://raw.githubusercontent.com/vadymkykalo/webhook-platform/main/install.sh | bash
#
# That writes a .env with real generated secrets, and only the handful of
# settings that a deployment actually needs to state. Everything else keeps the
# default baked into the Compose file — which is where the defaults below come
# from, so an unset variable here and a variable absent from .env mean the same
# thing.
#
# Deployment modes:
#   - embedded: PostgreSQL runs in Docker (self-hosted)
#   - external: Use external PostgreSQL (cloud/managed DB)

# ----------------------------------------------------------------------------
# DEPLOYMENT MODE (REQUIRED)
# ----------------------------------------------------------------------------
# Options: embedded | external
DB_MODE=embedded

# Docker registry prefix for pre-built images (used by docker-compose.yml).
# Defaults to this project's own published
# images if left unset — ghcr.io/vadymkykalo/hookflow-{api,worker,ui}.
# Set this to point at your own mirror/fork instead, e.g.:
# DOCKER_REGISTRY=ghcr.io/yourorg/webhook-platform
#   -> resolves to ghcr.io/yourorg/webhook-platform-api:latest

# ----------------------------------------------------------------------------
# APPLICATION ENVIRONMENT
# ----------------------------------------------------------------------------
APP_ENV=development
# Options: development | staging | production
# Note: In production mode, placeholder secrets will cause startup failure

# Public frontend URL (used for emails, shared debug links, etc.)
APP_BASE_URL=http://localhost:5173

# ----------------------------------------------------------------------------
# DATABASE - EMBEDDED MODE (when DB_MODE=embedded)
# ----------------------------------------------------------------------------
# Used when running PostgreSQL in Docker
POSTGRES_IMAGE=postgres:16-alpine
POSTGRES_DB=webhook_platform
POSTGRES_USER=webhook_user
POSTGRES_PASSWORD=webhook_dev_pass_12345
# POSTGRES_VOLUME_NAME is gone: it pinned one globally-named volume that every
# install on a host would share. The data volume is project-scoped now.
# POSTGRES_PORT is gone too: postgres publishes nothing to the host, so there was
# no mapping for it to move. Uncomment the example ports: block in
# docker-compose.yml if you want to reach the database from outside the network.

# ----------------------------------------------------------------------------
# DATABASE - EXTERNAL MODE (when DB_MODE=external)
# ----------------------------------------------------------------------------
# Used when connecting to external/managed PostgreSQL
# For embedded mode, leave as 'postgres' (Docker service name)
# For external mode, change to your database host
DB_HOST=postgres
DB_PORT=5432
DB_NAME=webhook_platform
DB_USER=webhook_user
DB_PASSWORD=webhook_dev_pass_12345
DB_SSL_MODE=disable
# Options: require | verify-full | disable

# Optional: Override full JDBC URL (if set, DB_HOST/PORT/NAME are ignored)
DB_JDBC_URL=

# Connection pool settings -- API only. The worker has its own WORKER_DB_POOL_MAX_SIZE /
# WORKER_DB_POOL_MIN_IDLE below: these two used to share this same name, which meant
# `make up` silently gave the worker the API's smaller pool instead of its own tuned one, no
# matter what either service's application.yml default claimed. Keep them separately named.
DB_POOL_MAX_SIZE=20
DB_POOL_MIN_IDLE=10
DB_POOL_CONNECTION_TIMEOUT=30000

# Worker connection pool settings. Sized against the number of transactions that can
# actually be holding a connection at once, not the worker's raw thread count -- the worker
# runs ~70 nominal concurrent tasks (webhook.outgoing-pool-size=50 + webhook.incoming-pool-size
# =20 + a handful of scheduler threads), but each delivery attempt's dominant cost is the
# outbound HTTP call, which holds NO connection (WebhookDeliveryService's transactions are the
# 3-4 single-row claim/status-transition updates around it, each open for single-digit
# milliseconds). Retry/incoming-forward claiming is also centralized in one scheduler
# transaction per cycle (RetrySchedulerService/IncomingForwardRetryScheduler batch-claim, then
# fan out to the executor pool for the HTTP work), not fanned out across all worker threads at
# once. 40/20 has margin over the realistic simultaneous-holder count under this shape; if a
# future change makes worker transactions span the HTTP call (it must not -- see
# WebhookDeliveryService's own comments) or the executor pool sizes grow substantially, re-check
# hikari_connections_pending on /actuator/prometheus under load before assuming this still holds.
WORKER_DB_POOL_MAX_SIZE=40
WORKER_DB_POOL_MIN_IDLE=20

# ----------------------------------------------------------------------------
# REDIS CONFIGURATION (for distributed rate limiting & concurrency)
# ----------------------------------------------------------------------------
REDIS_IMAGE=redis:7-alpine
REDIS_HOST=redis
REDIS_PORT=6379
REDIS_PASSWORD=webhook_redis_pass
REDIS_MAXMEMORY=256mb

# ----------------------------------------------------------------------------
# KAFKA CONFIGURATION
# ----------------------------------------------------------------------------
# For embedded mode, use kafka:9092 (Docker internal)
# For external mode, use your Kafka cluster URL
KAFKA_BOOTSTRAP_SERVERS=kafka:9092
KAFKA_IMAGE=apache/kafka:3.7.0
# Number of partitions for highload (increase for more parallelism)
KAFKA_NUM_PARTITIONS=12

# Consumer group IDs (prefix with env name on shared Kafka clusters, e.g. prod-webhook-worker)
KAFKA_CONSUMER_GROUP_ID=webhook-worker
KAFKA_INCOMING_GROUP_ID=incoming-forward-worker

# Worker consumer thread concurrency (should be ≤ KAFKA_NUM_PARTITIONS)
# Delivery consumer: outgoing webhook dispatch (higher = more throughput)
KAFKA_DELIVERY_CONCURRENCY=6
# Incoming forward consumer: incoming webhook forwarding
KAFKA_INCOMING_CONCURRENCY=3

# Kafka consumer settings (worker only)
KAFKA_CONSUMER_AUTO_OFFSET_RESET=earliest
KAFKA_CONSUMER_MAX_RETRIES=3
KAFKA_CONSUMER_RETRY_INTERVAL_MS=5000

# ----------------------------------------------------------------------------
# SECURITY (REQUIRED - MUST CHANGE FOR PRODUCTION)
# ----------------------------------------------------------------------------
# Webhook secret encryption key (32+ characters, alphanumeric + special chars)
# CHANGE THIS IN PRODUCTION!
WEBHOOK_ENCRYPTION_KEY=dev_encryption_key_32_chars_min!

# PBKDF2 salt for AES-256 key derivation (16+ characters, unique per deployment)
# CHANGE THIS IN PRODUCTION!
WEBHOOK_ENCRYPTION_SALT=dev_encryption_salt_16_chars

# Multi-key encryption for zero-downtime key rotation (optional)
# Format: "version:key,version:key" — e.g. "1:old_key,2:new_key"
# Leave empty to use single WEBHOOK_ENCRYPTION_KEY as version 1
WEBHOOK_ENCRYPTION_KEYS=
# Active key version for new encryptions (0 = auto-detect highest version)
WEBHOOK_ENCRYPTION_KEY_ACTIVE_VERSION=0

# JWT secret for API authentication (32+ characters)
# CHANGE THIS IN PRODUCTION!
JWT_SECRET=dev_jwt_secret_key_32_chars_minimum

# Platform-admin operator credential for cluster-wide operator endpoints
# (/api/v1/admin/**, e.g. encryption key rotation across ALL tenants).
# Deliberately independent of any org/JWT — org OWNER does NOT grant this.
# Presented via the X-Platform-Admin-Token header. Leave empty to keep the
# admin endpoints unreachable entirely (fail closed) until an operator needs them.
# CHANGE/SET THIS IN PRODUCTION before using the admin endpoints!
PLATFORM_ADMIN_TOKEN=

# ----------------------------------------------------------------------------
# API SERVICE
# ----------------------------------------------------------------------------
API_IMAGE_TAG=latest
# API_PORT and API_BIND are gone. The api service publishes no host port at all --
# every request reaches it through the UI's nginx on HOOKFLOW_PORT -- so the two
# variables described a mapping that does not exist, and "in production, keep
# 127.0.0.1" read as a hardening step that did nothing. To reach the api directly
# on a development box, uncomment the example ports: block in docker-compose.yml.

# ----------------------------------------------------------------------------
# WORKER SERVICE
# ----------------------------------------------------------------------------
WORKER_IMAGE_TAG=latest
# WORKER_PORT is gone for the same reason as API_PORT: the worker publishes
# nothing to the host.

# The actuator ports (api 8082, worker 8081) and their bind address are no
# longer configurable, and are set literally in the Compose files. Neither is
# published to the host, so there was nothing to tune — only a way to break
# Prometheus and the health endpoint by binding them to container loopback,
# which happened.

# ----------------------------------------------------------------------------
# SCHEDULING (api + worker)
# ----------------------------------------------------------------------------
# Size of the shared @Scheduled thread pool. Spring Boot defaults this to 1, which
# means a single slow cron (e.g. the materialized-view REFRESH, or an unbounded
# Kafka AdminClient call) blocks every other job on the JVM - including outbox
# dispatch. Keep at 8 or higher.
SCHEDULING_POOL_SIZE=8

# ----------------------------------------------------------------------------
# UI SERVICE
# ----------------------------------------------------------------------------
# The one port published to the host: the dashboard's
# nginx, which also proxies every API path. Set HOOKFLOW_BIND=127.0.0.1 and let a
# reverse proxy in front hold 443 — that is what installing with --domain does.
# 8080 rather than 80 because this template is what a clone runs, and a
# developer's port 80 usually belongs to something else already. install.sh
# writes 80 into the .env it generates, since that is what a browser assumes.
HOOKFLOW_PORT=8080
HOOKFLOW_BIND=0.0.0.0

# Domain and ACME contact for the built-in TLS terminator. It only runs under
# the `tls` Compose profile; COMPOSE_PROFILES=tls turns it on.
HOOKFLOW_DOMAIN=
ACME_EMAIL=

# UI_PORT is gone: the dashboard is published on HOOKFLOW_PORT above, by both the
# published-image path and the build overlay. Nothing read UI_PORT.
UI_IMAGE_TAG=latest
# BUILD-TIME ONLY. Vite inlines VITE_* into the bundle, so these are passed as
# build args (docker-compose.yml -> ui.build.args) and take effect only when the
# UI image is rebuilt. On the pre-built images they
# cannot be changed at all.
#
# Leave VITE_API_URL empty: the UI's nginx proxies /api/, /ws/tunnel, /hook/ and
# /ingress/ to the api service, so the browser talks to its own origin and needs
# no CORS. Set it only when the API is genuinely served from another origin —
# then add that origin to CORS_ALLOWED_ORIGINS too.
VITE_API_URL=
# Extra origins for CSP connect-src (space-separated). Auto-includes VITE_API_URL + localhost in dev.
# Example for prod: VITE_CSP_EXTRA_CONNECT=https://api.hookflow.dev wss://api.hookflow.dev
VITE_CSP_EXTRA_CONNECT=

# ----------------------------------------------------------------------------
# OUTBOX PUBLISHER
# ----------------------------------------------------------------------------
# Poll interval for pending outbox messages (ms)
OUTBOX_POLL_INTERVAL_MS=1000
# Retry interval for failed outbox messages (ms)
OUTBOX_RETRY_INTERVAL_MS=30000
# Cleanup interval for PUBLISHED/DEAD outbox messages (ms, default 1 hour)
OUTBOX_CLEANUP_INTERVAL_MS=3600000
# Batch size per poll cycle
OUTBOX_BATCH_SIZE=100
# Max publish retries before moving to DEAD status
OUTBOX_MAX_RETRIES=5
# Days to retain DEAD messages before deletion (allows manual re-drive)
OUTBOX_DEAD_RETENTION_DAYS=90
# Seconds before recovering stuck SENDING messages back to PENDING (must exceed Kafka delivery.timeout.ms)
OUTBOX_SENDING_RECOVERY_SECONDS=300
# Timeout in seconds for waiting on batch Kafka send completion
OUTBOX_BATCH_SEND_TIMEOUT_SECONDS=30
# Max outbox messages claimed per project per batch (prevents noisy-neighbor in publishing)
OUTBOX_MAX_PER_PROJECT=30

# DATA RETENTION POLICIES
# ----------------------------------------------------------------------------
# Published outbox rows are not tuned here: OutboxPublisherService owns that
# cleanup and reads OUTBOX_DEAD_RETENTION_DAYS above.
#
# Days to keep delivery attempts (cleanup runs daily at 2 AM)
# Differential retention: successful attempts (2xx) deleted faster, errors kept longer for debugging
DATA_RETENTION_ATTEMPTS_DAYS=90
DATA_RETENTION_SUCCESSFUL_ATTEMPTS_DAYS=14
DATA_RETENTION_INCOMING_EVENTS_DAYS=30

# How long an Event — and, through ON DELETE CASCADE, its Deliveries and their
# Attempts — is kept. Set to -1 to keep everything forever.
#
# This is the only setting that bounds the two largest tables. Per-plan retention
# (RetentionCleanupScheduler) does nothing while BILLING_ENABLED=false, which is
# the self-hosted default, so before this existed events and deliveries grew
# without limit while delivery_attempts partitions were still dropped at
# DATA_RETENTION_ATTEMPTS_DAYS — losing the detail and keeping the bulk.
#
# 90 matches DATA_RETENTION_ATTEMPTS_DAYS on purpose: keeping a delivery longer
# than the attempts that explain it buys rows nobody can act on.
DATA_RETENTION_EVENTS_DAYS=90

# Maximum attempts to retain per delivery (keeps most recent N)
DATA_RETENTION_MAX_ATTEMPTS_PER_DELIVERY=10

# Batch size for cleanup operations
DATA_RETENTION_BATCH_SIZE=1000

# Cron schedule for cleanup (daily at 2 AM)
DATA_RETENTION_CRON=0 0 2 * * *

# Cron schedule for limit enforcement (every 30 minutes)
DATA_RETENTION_LIMIT_CRON=0 */30 * * * *

# Burst cleanup cron — catches daytime error spikes (every 4 hours)
DATA_RETENTION_BURST_CLEANUP_CRON=0 0 */4 * * *

# How often to refresh table size gauges (ms, default 15 min)
DATA_RETENTION_TABLE_METRICS_INTERVAL_MS=900000

# PARTITION MAINTENANCE (delivery_attempts, tunnel_request_log — see V052/V053
# migrations and PartitionMaintenanceService). Creates partitions ahead of need and
# drops ones fully past the DATA_RETENTION_* days above (O(1) DROP TABLE instead of
# DELETE). Safe to leave at the defaults for self-hosters.
# ----------------------------------------------------------------------------
PARTITION_MAINTENANCE_ENABLED=true
PARTITION_MAINTENANCE_DELIVERY_ATTEMPTS_LOOKAHEAD_MONTHS=3
PARTITION_MAINTENANCE_TUNNEL_LOG_LOOKAHEAD_WEEKS=3
PARTITION_MAINTENANCE_CRON=0 45 1 * * *

# ----------------------------------------------------------------------------
# RATE LIMITING
# ----------------------------------------------------------------------------
# Global platform rate limit (safety net, per instance, requests/sec)
GLOBAL_RATE_LIMIT_PER_SECOND=5000
GLOBAL_RATE_LIMIT_ENABLED=true

# Events per second per project (token bucket)
EVENT_INGESTION_RATE_LIMIT_PER_SECOND=100

# Auth rate limiting (per IP and per email, per minute)
AUTH_RATE_LIMIT_LOGIN_PER_MINUTE=10
AUTH_RATE_LIMIT_REGISTER_PER_MINUTE=5

# ----------------------------------------------------------------------------
# PASSWORD AUTHENTICATION
# ----------------------------------------------------------------------------
# BCrypt work factor for stored password hashes. A doubling scale: each step is
# twice the work for an attacker grinding a leaked users table, and twice the
# work for one login. 12 is roughly a quarter-second on a typical server, paid
# once per sign-in. BCrypt records the cost inside each hash, so raising or
# lowering this never invalidates hashes already stored -- existing passwords
# keep verifying at whatever cost they were written with, and are rewritten at
# the new one the next time they change. Range 4-31; the API refuses to start
# outside it.
AUTH_BCRYPT_STRENGTH=12

# Account lockout after consecutive failed sign-ins. Rate limiting bounds how
# FAST an account can be guessed at; this bounds HOW MANY. It is held in
# Postgres rather than Redis on purpose: the auth rate limiter falls back to a
# per-instance in-memory bucket when Redis is down, which multiplies its real
# ceiling by the replica count, and a lockout with that property is not one.
#
# Every lockout expires by itself and a password reset clears it immediately,
# so locking a known email address costs its owner minutes, not access -- see
# AccountLockoutService for the full reasoning. Set AUTH_LOCKOUT_ENABLED=false
# only if something in front of the API already bounds attempts per account.
AUTH_LOCKOUT_ENABLED=true
# Consecutive failures before the account locks.
AUTH_LOCKOUT_THRESHOLD=5
# First lockout, in seconds. Doubles with each further failure.
AUTH_LOCKOUT_INITIAL_SECONDS=60
# Cap on the doubling. Beyond about this, a longer window stops helping a
# defender and only helps somebody locking a stranger out.
AUTH_LOCKOUT_MAX_SECONDS=900
# Failures older than this are not "consecutive" any more and are forgotten,
# so occasional typos months apart never add up to a lockout.
AUTH_LOCKOUT_FAILURE_WINDOW_MINUTES=60

# How often expired user_sessions rows are purged. Housekeeping only: an expired
# session cannot authenticate anything either way.
AUTH_SESSION_CLEANUP_INTERVAL_MS=3600000

# ----------------------------------------------------------------------------
# WEBHOOK DELIVERY
# ----------------------------------------------------------------------------
# Maximum event payload size in bytes (default 256KB)
WEBHOOK_MAX_PAYLOAD_SIZE_BYTES=262144

# Max deliveries per single event (fanout limit — prevents queue flood)
WEBHOOK_MAX_FANOUT_PER_EVENT=100

# Payload compression threshold - payloads larger than this will be gzip compressed (default 1KB)
# Reduces DB storage significantly for large payloads
WEBHOOK_PAYLOAD_COMPRESSION_THRESHOLD_BYTES=1024

# Per-project delivery rate limit (deliveries/sec) — prevents noisy-neighbor
WEBHOOK_PROJECT_RATE_LIMIT_PER_SECOND=50

# Require endpoint verification before allowing deliveries (default: false for backward compatibility)
# Set to true to enforce proof-of-ownership for all new endpoints
WEBHOOK_ENDPOINT_VERIFICATION_REQUIRED=false

# How long a received signature is remembered, so the same webhook is not processed twice.
# For a provider whose signature carries a timestamp this is duplicate suppression and five
# minutes is generous. For a source verified with a raw-hex generic HMAC it is the only bound
# on replay: that shape signs the body alone, so a captured request stays verifiable for as
# long as the secret lives and nothing else stops it being sent again. Raise it for such a
# source, at the cost of Redis holding one key per signature for the window.
WEBHOOK_INGRESS_REPLAY_WINDOW_MINUTES=5

# Allow webhooks to private IPs. Off by default (P0-14c) — a self-hoster who copies
# .env.dist -> .env and runs `make up` should not silently expose an SSRF hole
# (e.g. an endpoint pointed at http://169.254.169.254/ reading cloud metadata).
# Set to true only if you specifically need to forward to internal/private targets
# in local development, and never in production (ProductionSafetyValidator enforces
# this at startup when APP_ENV=production).
WEBHOOK_ALLOW_PRIVATE_IPS=false

# Max concurrent deliveries per endpoint (prevents endpoint overload)
WEBHOOK_MAX_CONCURRENT_PER_ENDPOINT=5

# The most attempts one ORGANIZATION may have in flight at once, across every
# endpoint it owns. The per-endpoint cap above bounds one receiver to a slice of
# WEBHOOK_OUTGOING_POOL_SIZE; on its own that does nothing about a tenant with
# many slow receivers, because each stays inside its own slice and their sum is
# the whole worker — at which point no other organization is delivered to at all.
#
# A permit is held only while a request is actually in flight, so healthy traffic
# never reaches this: fast endpoints hand permits straight back. It bites exactly
# when calls are slow, which is when it should.
#
# Sized against WEBHOOK_OUTGOING_POOL_SIZE: 20 of 50 lets a single tenant use most
# of an idle worker while leaving room for two more to be busy at the same time.
# Raise it if you run one large tenant; lower it if you run many small ones.
WEBHOOK_MAX_CONCURRENT_PER_TENANT=20

# Lease time (seconds) on a concurrency permit — bounds how long a crashed/stuck delivery
# can hold a permit before Redis reclaims it. Must exceed the max per-delivery HTTP timeout
# (60s, see clampTimeout) with margin for pre-request work (decrypt, mTLS setup, transform).
WEBHOOK_CONCURRENCY_PERMIT_LEASE_SECONDS=90

# Outgoing webhook delivery pool (isolated from incoming forwards)
WEBHOOK_OUTGOING_POOL_SIZE=50

# Incoming forward pool (isolated from outgoing deliveries)
WEBHOOK_INCOMING_POOL_SIZE=20

# Graceful shutdown timeout for in-flight deliveries (seconds)
WEBHOOK_ASYNC_SHUTDOWN_TIMEOUT_SECONDS=60

# HTTP connection pool for outgoing webhook delivery (shared by delivery + incoming forward)
WEBHOOK_POOL_MAX_CONNECTIONS=200
WEBHOOK_POOL_PENDING_ACQUIRE_TIMEOUT_SECONDS=10
WEBHOOK_POOL_MAX_IDLE_TIME_SECONDS=60

# Hosts exempted from the private-IP check, comma-separated. Empty is the default and
# means no exemption - not "all allowed": a public host needs no entry here, and every
# other rule (scheme, cloud-metadata denylist) applies to a listed host regardless.
#
# This is the supported way to forward to one named internal service while leaving
# WEBHOOK_ALLOW_PRIVATE_IPS=false everywhere else. Entries are matched against the host
# as written in the endpoint URL, so a name, not the address it resolves to.
WEBHOOK_ALLOWED_HOSTS=

# Trusted reverse-proxy peers (comma-separated IPs and/or CIDR ranges), e.g.
#   WEBHOOK_TRUSTED_PROXIES=10.0.0.5,172.20.0.0/16
# X-Forwarded-For / X-Real-IP are honoured ONLY when the direct TCP connection to the
# API comes from one of these addresses; the header is otherwise ignored and the real
# socket peer address is used. This governs the IP used for auth rate limiting
# (login/register/refresh/reset-password), audit logs, and incoming-webhook source IP
# checks.
#
# Empty (default) trusts nothing — safe whenever the API is reachable directly, as in
# the base docker-compose.yml here. If you put a reverse proxy, load balancer, or
# ingress controller in front (as the UI's nginx does), set
# this to that proxy's address/CIDR, or every request will be bucketed under the
# proxy's own IP instead of the real client's — getting this wrong in either direction
# either defeats rate limiting (trusting an untrusted peer) or breaks it for everyone
# behind one shared IP (trusting nothing when you should).
WEBHOOK_TRUSTED_PROXIES=

# ----------------------------------------------------------------------------
# INCOMING WEBHOOKS
# ----------------------------------------------------------------------------
# Base URL for ingress endpoint URLs shown in admin UI
WEBHOOK_INGRESS_BASE_URL=http://localhost:8080

# Maximum incoming payload size in bytes (default 512KB)
WEBHOOK_INCOMING_MAX_PAYLOAD_SIZE_BYTES=524288

# Per-source ingress rate limit for a source that sets none of its own. Without it the only
# backstop on the public /ingress/{token} path was the single platform-wide bucket
# (GLOBAL_RATE_LIMIT_PER_SECOND), shared by every tenant. Kept equal to
# EVENT_INGESTION_RATE_LIMIT_PER_SECOND: a received webhook costs what an ingested event costs.
# Set to 0 to disable the default (a source's own limit still applies).
WEBHOOK_INCOMING_RATE_LIMIT_PER_SECOND=100

# Incoming forward retry scheduler
INCOMING_FORWARD_RETRY_POLL_INTERVAL_MS=10000
INCOMING_FORWARD_RETRY_BATCH_SIZE=50
INCOMING_FORWARD_RETRY_MAX_PER_DEST=10

# Queue depth threshold for incoming forward retry governor
INCOMING_FORWARD_RETRY_HIGH_WATERMARK=3000

# DLQ monitoring interval (ms)
DLQ_MONITORING_INTERVAL_MS=60000
# Timeout for the Kafka AdminClient calls DLQ monitoring makes each cycle (seconds)
DLQ_MONITORING_ADMIN_CLIENT_TIMEOUT_SECONDS=10

# ----------------------------------------------------------------------------
# ORDERING (FIFO delivery per endpoint)
# ----------------------------------------------------------------------------
# Gap timeout in seconds (proceed without previous sequence after this time)
ORDERING_GAP_TIMEOUT_SECONDS=60

# Fallback poll interval (seconds) for a delivery parked behind an outstanding sequence.
# The fast path republishes a buffered delivery the moment the sequence ahead of it
# completes; this only covers deliveries that reach the buffer after that trigger fired.
# Lower drains an out-of-order burst faster at the cost of more DB polling.
ORDERING_BUFFER_RESCHEDULE_DELAY_SECONDS=5

# TTL for delivered sequence tracking in Redis (hours)
ORDERING_DELIVERED_SEQ_TTL_HOURS=24

# TTL for ordering buffer in Redis (minutes)
ORDERING_BUFFER_TTL_MINUTES=10

# How often webhook_ordering_buffer_size resyncs against Redis via a key-pattern SCAN (ms).
# See OrderingBufferService#resyncBufferSizeGauge -- this replaced a per-call gauge
# registration that only ever reported the first endpoint's buffer, forever.
ORDERING_BUFFER_GAUGE_RESYNC_MS=30000

# How far back the API's periodic sequence/cursor reconciliation job looks for
# recently-active ordering-enabled endpoints (hours)
ORDERING_SEQUENCE_RECONCILIATION_LOOKBACK_HOURS=48

# How often the API's sequence/cursor reconciliation job runs (milliseconds)
ORDERING_SEQUENCE_RECONCILIATION_INTERVAL_MS=900000

# ----------------------------------------------------------------------------
# EVENT TIME MACHINE (REPLAY)
# ----------------------------------------------------------------------------
# Batch size for cursor-based event scanning (tune for DB load)
REPLAY_BATCH_SIZE=200

# Delay in ms between batches (backpressure against Kafka/DB)
REPLAY_BATCH_DELAY_MS=50

# Maximum events per single replay session
REPLAY_MAX_EVENTS_PER_SESSION=500000

# ----------------------------------------------------------------------------
# RETRY SCHEDULER
# ----------------------------------------------------------------------------
# Batch size for processing pending retries (governor ceiling — actual batch adapts dynamically)
RETRY_SCHEDULER_BATCH_SIZE=100

# Max retries claimed per endpoint per poll (fair scheduling)
RETRY_SCHEDULER_MAX_PER_ENDPOINT=10

# Max retries claimed per project per poll (prevents noisy-neighbor in retry queue)
RETRY_SCHEDULER_MAX_PER_PROJECT=30

# Queue depth threshold — when pending retries exceed this, governor caps batch size
# to prevent retry storm after outages (AIMD congestion control)
RETRY_SCHEDULER_HIGH_WATERMARK=5000

# Timeout in seconds for Kafka send confirmation
RETRY_SCHEDULER_SEND_TIMEOUT_SECONDS=30

# Delay in seconds before rescheduling failed sends
RETRY_SCHEDULER_RESCHEDULE_DELAY_SECONDS=60

# Poll interval in milliseconds. This is the baseline the adaptive governor scales
# from, not a fixed period: it polls at this rate under light load, backs off to 3x
# when the retry queue is empty, and tightens to 1/2x and 1/5x as the backlog grows.
RETRY_SCHEDULER_POLL_INTERVAL_MS=10000

# The default retry ladder is NOT configurable by environment variable. It is declared in
# RetryLadderDefaults and mirrored by the Flyway column defaults, and differs per direction
# by design: outgoing gets 60,300,900,3600,21600,86400 over 7 attempts, incoming gets
# 60,300,900,3600,21600 over 5. Per-subscription and per-destination ladders are set through
# the API and stored on the row; the API rejects a malformed ladder with a 400.
#
# RETRY_LADDER_DEFAULT_DELAYS_SECONDS and RETRY_LADDER_DEFAULT_MAX_ATTEMPTS used to sit here
# and were removed — see UPGRADING.md. They read as though they set the default ladder but
# only changed what the startup cap check compared against, so lowering one made the check
# pass while live rows still carried the long ladder.

# ----------------------------------------------------------------------------
# STUCK DELIVERY RECOVERY
# ----------------------------------------------------------------------------
# Threshold in minutes - deliveries stuck in PROCESSING longer than this will be reset
STUCK_DELIVERY_THRESHOLD_MINUTES=5

# Threshold in minutes - PENDING deliveries with next_retry_at wiped but never claimed
# into PROCESSING (belt-and-braces recovery) will be reset. Keep well above the normal
# dispatch window so freshly ingested deliveries are never swept.
STUCK_DELIVERY_STRANDED_PENDING_THRESHOLD_MINUTES=60

# Check interval in milliseconds
STUCK_DELIVERY_CHECK_INTERVAL_MS=60000

# ----------------------------------------------------------------------------
# STUCK FORWARD RECOVERY (incoming forward attempts)
# ----------------------------------------------------------------------------
# Threshold in minutes - forward attempts stuck in PROCESSING longer than this will be reset
STUCK_FORWARD_THRESHOLD_MINUTES=5

# Threshold in minutes - forward attempts left PENDING with next_retry_at never set (the
# dispatch or replay Kafka message was lost) will be handed back to the retry scheduler.
# Keep well above the normal dispatch window so freshly received webhooks are never swept.
STUCK_FORWARD_STRANDED_PENDING_THRESHOLD_MINUTES=60

# Check interval in milliseconds
STUCK_FORWARD_CHECK_INTERVAL_MS=60000

# ----------------------------------------------------------------------------
# CIRCUIT BREAKER (Redis-backed, shared across worker pods)
# ----------------------------------------------------------------------------
# Failure rate percentage threshold to trip the circuit (0-100)
CIRCUIT_BREAKER_FAILURE_RATE_THRESHOLD=50

# Minimum number of calls before evaluating failure rate
CIRCUIT_BREAKER_MINIMUM_CALLS=5

# How long the circuit stays OPEN before allowing probes (seconds)
CIRCUIT_BREAKER_WAIT_DURATION_SECONDS=30

# TTL for failure/call counters sliding window (seconds)
CIRCUIT_BREAKER_WINDOW_TTL_SECONDS=120

# Latency threshold to classify a call as "slow" (ms). Endpoints consistently
# responding slower than this will trip the circuit even if they return 2xx.
CIRCUIT_BREAKER_SLOW_CALL_THRESHOLD_MS=10000

# Percentage of slow calls to trip the circuit (0-100)
CIRCUIT_BREAKER_SLOW_CALL_RATE_THRESHOLD=80

# ----------------------------------------------------------------------------
# STALE DELIVERY ESCALATION (hard-cap auto-DLQ)
# ----------------------------------------------------------------------------
# Deliveries stuck in PENDING longer than this are escalated to DLQ (hours), regardless of
# attempt_count/max_attempts. Must stay >= the worst-case span of BOTH default ladders (see
# RetryLadderDefaults) or the last retry tiers never get a chance to fire —
# RetrySchedulerService fails startup if they don't agree. The outgoing ladder's worst case
# with full jitter is ~83h; 96h leaves headroom.
DELIVERY_ESCALATION_HARD_CAP_HOURS=96

# Batch size per escalation cycle
DELIVERY_ESCALATION_BATCH_SIZE=100

# ----------------------------------------------------------------------------
# STALE FORWARD ESCALATION (hard-cap auto-DLQ, Incoming)
# ----------------------------------------------------------------------------
# The Incoming counterpart of DELIVERY_ESCALATION_HARD_CAP_HOURS. Forwards outstanding longer
# than this — measured from when the webhook arrived, not from the newest attempt row — are
# escalated to DLQ regardless of attempt count. Before this existed the Incoming direction had
# only a stuck-PROCESSING reset and never gave up on anything.
#
# Deliberately its own cap rather than reusing the delivery one: the incoming ladder is shorter
# by design (5 attempts to 6h, ~11h worst case with full jitter), so the 96h delivery cap would
# leave a dead Forward sitting for three days past ladder exhaustion. RetrySchedulerService
# fails startup if the incoming ladder does not fit inside this.
FORWARD_ESCALATION_HARD_CAP_HOURS=24
FORWARD_ESCALATION_BATCH_SIZE=100
FORWARD_ESCALATION_INTERVAL_MS=300000

# How often to check for stale deliveries (ms, default 5 min)
DELIVERY_ESCALATION_INTERVAL_MS=300000

# ----------------------------------------------------------------------------
# QUEUE DEPTH METRICS
# ----------------------------------------------------------------------------
# How often to refresh queue depth gauges (ms) — default 15 min
QUEUE_DEPTH_METRICS_INTERVAL_MS=900000

# Only count records created within this many days (limits scan scope)
QUEUE_DEPTH_METRICS_RETENTION_DAYS=30

# ----------------------------------------------------------------------------
# TEST ENDPOINTS (Request Bin)
# ----------------------------------------------------------------------------
# Base URL for generated test endpoint URLs
TEST_ENDPOINT_BASE_URL=http://api:8080

# Maximum test endpoints per project
TEST_ENDPOINT_MAX_PER_PROJECT=10

# Maximum TTL in hours for test endpoints
TEST_ENDPOINT_MAX_TTL_HOURS=72

# Cleanup interval for expired endpoints (ms)
TEST_ENDPOINT_CLEANUP_INTERVAL_MS=3600000

# ----------------------------------------------------------------------------
# RULES ENGINE
# ----------------------------------------------------------------------------
# Cache refresh interval for compiled rules (ms, default 30s)
RULES_CACHE_REFRESH_MS=30000

# Retention for rule execution logs (days, default 7)
RULES_EXECUTION_LOG_RETENTION_DAYS=7

# ----------------------------------------------------------------------------
# WORKFLOW ENGINE
# ----------------------------------------------------------------------------
# Thread pool for async workflow execution
WORKFLOW_POOL_CORE_SIZE=4
WORKFLOW_POOL_MAX_SIZE=8
WORKFLOW_POOL_QUEUE_CAPACITY=50

# Maximum total execution time for a single workflow run (seconds)
WORKFLOW_MAX_DURATION_SECONDS=600

# Maximum recursion depth for workflow chains (workflow→CreateEvent→workflow)
WORKFLOW_MAX_RECURSION_DEPTH=3

# Stuck execution recovery: threshold (minutes) and check interval (ms)
WORKFLOW_STUCK_THRESHOLD_MINUTES=15
WORKFLOW_RECOVERY_INTERVAL_MS=120000

# A delay node suspends its execution rather than sleeping on a pool thread, and
# this is how often suspended executions are checked for being due. It is the
# resolution of a delay, so a delay is always "at least N seconds", never
# exactly N — which is what a delay in a workflow means anyway.
WORKFLOW_RESUME_INTERVAL_MS=5000

# How many due executions one tick resumes. Matters more than the interval: a
# burst all becoming due in the same second must not hand the workflow pool more
# than it can take, so the remainder spills to the next tick in due order.
WORKFLOW_RESUME_BATCH_SIZE=50

# Per-node timeout limits (seconds)
WORKFLOW_NODE_TIMEOUT_DEFAULT_SECONDS=30
WORKFLOW_NODE_TIMEOUT_HTTP_SECONDS=60
WORKFLOW_NODE_TIMEOUT_SLACK_SECONDS=60
WORKFLOW_NODE_TIMEOUT_CREATE_EVENT_SECONDS=30
WORKFLOW_NODE_TIMEOUT_POOL_SIZE=16

# Graceful shutdown: how long to wait for in-flight workflows (seconds)
WORKFLOW_SHUTDOWN_AWAIT_SECONDS=30

# Workflow trigger outbox (durable at-least-once trigger via transactional outbox)
WORKFLOW_TRIGGER_OUTBOX_POLL_INTERVAL_MS=2000
WORKFLOW_TRIGGER_OUTBOX_BATCH_SIZE=50
WORKFLOW_TRIGGER_OUTBOX_MAX_ATTEMPTS=3
# Max workflow triggers claimed per project per batch (prevents one project from starving others)
WORKFLOW_TRIGGER_OUTBOX_MAX_PER_PROJECT=5
# Max concurrent workflow executions per project (prevents executor thread exhaustion)
WORKFLOW_TRIGGER_OUTBOX_MAX_CONCURRENT_PER_PROJECT=3
# A claimed row goes PROCESSING and only its own poller can move it on. If that poller dies
# mid-workflow, nothing else picks the row up: the batch claim reads PENDING and the cleanup
# job deletes DONE. This sweep returns rows abandoned in PROCESSING to the queue. Keep the
# threshold above the longest workflow you actually run, or a live one gets reclaimed
# underneath itself and executes twice.
WORKFLOW_TRIGGER_OUTBOX_STALLED_AFTER_MINUTES=15
WORKFLOW_TRIGGER_OUTBOX_STALLED_SWEEP_MS=300000

# ----------------------------------------------------------------------------
# DEVICE AUTHORIZATION
# ----------------------------------------------------------------------------
# Cleanup interval for expired device auth codes (ms)
DEVICE_AUTH_CLEANUP_INTERVAL_MS=300000

# ----------------------------------------------------------------------------
# EMAIL VERIFICATION
# ----------------------------------------------------------------------------
# Enable sending real emails (false = log verification URL to console).
#
# This also decides whether email verification is enforced at all. With it off, a new
# account is marked verified the moment it registers: there is no channel that could carry
# a token to the address, and a gate with no key is just a locked-out user. With it on,
# registration leaves the account unverified and the API refuses every write from it until
# the address is confirmed -- reads still work, so the dashboard can say so.
#
# Turn it on for any deployment where registration is open to strangers. It is the
# difference between an address someone owns and an address someone typed.
EMAIL_ENABLED=false

# From address for verification emails
EMAIL_FROM=noreply@hookflow.dev

# SMTP settings (only used when EMAIL_ENABLED=true)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USERNAME=
SMTP_PASSWORD=
SMTP_AUTH=false
SMTP_STARTTLS=false

# ----------------------------------------------------------------------------
# ALERT NOTIFICATIONS
# ----------------------------------------------------------------------------
# Enable sending real alert notifications via Slack/Webhook/Email
# (false = dry-run mode, logs notification details to console)
ALERTS_NOTIFICATIONS_ENABLED=false

# How often alert rules are evaluated. Every rule is measured over its own
# window, so this is the resolution of "how late can an alert be", not the
# window itself. A rule fires once per crossing and stays quiet until its
# event is resolved, so a faster cadence does not mean more notifications.
ALERTS_EVALUATION_CRON=0 * * * * *

# ----------------------------------------------------------------------------
# BILLING / PLANS
# ----------------------------------------------------------------------------
# Default entitlement limits when BILLING_ENABLED=false (self-hosted sane defaults)
# Prevents Integer.MAX_VALUE (effectively no limit) which can cause cascading failures
ENTITLEMENT_DEFAULT_RATE_LIMIT=100
ENTITLEMENT_DEFAULT_MAX_FANOUT=50

# Enable plan-based quota enforcement (false = self-hosted, all unlimited)
BILLING_ENABLED=false

# Default billing provider: noop | stripe | wayforpay
# When BILLING_ENABLED=false, provider is ignored (NoOp used automatically)
BILLING_DEFAULT_PROVIDER=noop

# Stripe settings (registered automatically when STRIPE_SECRET_KEY is set)
# STRIPE_SECRET_KEY=sk_test_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# Format: plan_interval=stripe_price_id (interval: _monthly or _yearly)
# STRIPE_PRICE_MAP=starter_monthly=price_xxx,starter_yearly=price_xxy,pro_monthly=price_yyy,pro_yearly=price_yyz

# Billing reconciliation — syncs local state with external provider (Stripe only).
# WayForPay is merchant-initiated so our local state IS the source of truth.
# Cron: every 6 hours by default.
BILLING_RECONCILIATION_CRON=0 0 */6 * * *

# WayForPay settings (registered automatically when WAYFORPAY_MERCHANT_ACCOUNT is set)
# WAYFORPAY_MERCHANT_ACCOUNT=your_merchant_account
# WAYFORPAY_MERCHANT_SECRET=your_merchant_secret_key
# WAYFORPAY_MERCHANT_DOMAIN=hookflow.dev
# WAYFORPAY_SERVICE_URL=https://hookflow.dev/api/v1/billing/webhook/wayforpay
# Format: plan_interval=amount_in_cents (interval: _monthly or _yearly)
# WAYFORPAY_PLAN_PRICES=starter_monthly=2900,starter_yearly=29000,pro_monthly=9900,pro_yearly=99000

# The locale the UI's public pages are rendered into static HTML as, at image build
# time. There is one set of URLs for two locales — the language is chosen client-side —
# so this has to be the one the canonical tags and sitemap describe. Build-time only:
# it has no effect on a running container.
PRERENDER_LOCALE=en

# ----------------------------------------------------------------------------
# TUNNEL (CLI local webhook tunnels)
# ----------------------------------------------------------------------------
# Public base URL for tunnel ingress (used to generate public tunnel URLs)
# In production: set to your actual domain, e.g. https://tunnels.hookflow.dev
TUNNEL_INGRESS_BASE_URL=http://localhost:8080

# Heartbeat timeout — tunnel session marked stale after this many seconds without heartbeat
TUNNEL_HEARTBEAT_TIMEOUT_SECONDS=120

# Stale session cleanup interval (ms)
TUNNEL_CLEANUP_INTERVAL_MS=60000

# Tunnel request log retention (days). tunnel_request_log is partitioned weekly,
# so retention is a DROP TABLE of whole expired partitions by the partition
# maintenance job (PARTITION_MAINTENANCE_CRON), not a row-by-row delete of its own.
TUNNEL_REQUEST_LOG_RETENTION_DAYS=7

# ----------------------------------------------------------------------------
# CORS CONFIGURATION
# ----------------------------------------------------------------------------
# The stack is served on HOOKFLOW_PORT, so that origin has to be here: the
# browser sends `Origin` on same-origin POSTs too, and Spring's allow-list
# rejects anything it does not list — which made a template-default install
# answer every login with "Invalid CORS request". 5173 is the Vite dev server
# for `npm run dev`; nothing in this stack has ever served on 3000.
CORS_ALLOWED_ORIGINS=http://localhost:8080,http://localhost:5173

# ----------------------------------------------------------------------------
# OBSERVABILITY
# ----------------------------------------------------------------------------
# Log level: TRACE | DEBUG | INFO | WARN | ERROR
LOG_LEVEL=INFO

# Enable/disable Swagger UI (set to false in production)
SWAGGER_ENABLED=false

# ----------------------------------------------------------------------------
# JVM TUNING
# ----------------------------------------------------------------------------
# Extra JVM flags, APPENDED to the tuning baked into the images
# (G1GC, UseContainerSupport, MaxRAMPercentage=75%, InitialRAMPercentage=50%).
# They append rather than replace: an explicit -Xmx here still wins over
# MaxRAMPercentage, but leaving these unset cannot erase the container tuning.
# API_JAVA_OPTS=-Xms256m -Xmx384m
# WORKER_JAVA_OPTS=-Xms384m -Xmx512m

# Enable Prometheus metrics export
METRICS_ENABLED=true

# ----------------------------------------------------------------------------
# RESOURCE LIMITS (Docker deploy.resources)
# ----------------------------------------------------------------------------
# Override memory limits per service (used in docker-compose.yml)
# POSTGRES_MEMORY_LIMIT=1G
# KAFKA_MEMORY_LIMIT=1G
# REDIS_MEMORY_LIMIT=512m
# API_MEMORY_LIMIT=768m
# WORKER_MEMORY_LIMIT=768m
# UI_MEMORY_LIMIT=128m

# ----------------------------------------------------------------------------
# ALERTING (monitoring/alertmanager — see monitoring/README.md) — optional
# ----------------------------------------------------------------------------
# Consumed by monitoring/alertmanager/alertmanager.yml via envsubst at
# container start (see monitoring/docker-compose.yml). Leave unset and
# Alertmanager still runs — alerts just land only in its own UI
# (http://localhost:9093) and the null "blackhole" receiver, which is exactly
# as useless as having no Alertmanager, so set at least one of these for a real
# deployment.
#
# Slack: an "Incoming Webhook" URL from https://api.slack.com/messaging/webhooks
ALERTMANAGER_SLACK_WEBHOOK_URL=
ALERTMANAGER_SLACK_CHANNEL=#hookflow-alerts
# Generic webhook receiver (e.g. PagerDuty Events API v2, Opsgenie, a custom sink)
ALERTMANAGER_WEBHOOK_URL=
# Email receiver — set these to the same values as SMTP_* above if you want
# alert emails to go through the same relay as transactional email
ALERTMANAGER_EMAIL_TO=
ALERTMANAGER_EMAIL_FROM=alerts@hookflow.dev
ALERTMANAGER_SMTP_HOST=localhost
ALERTMANAGER_SMTP_PORT=1025

# ----------------------------------------------------------------------------
# LOG AGGREGATION (monitoring/loki, monitoring/promtail — see monitoring/README.md)
# ----------------------------------------------------------------------------
# How long Loki keeps ingested log lines before compacting them away (Go duration,
# e.g. 336h = 14d). Independent of the DATA_RETENTION_* days above — those govern
# Postgres tables, this governs the separate log store, so a self-hoster's disk
# doesn't fill silently.
LOKI_RETENTION_PERIOD=336h

# ----------------------------------------------------------------------------
# BACKUP CONFIGURATION
# ----------------------------------------------------------------------------
# Used by `make backup-db` / `make restore-db` (embedded and external DB modes,
# see deploy/scripts/db-backup.sh) and by the `db-backup` Compose service, which
# runs a backup on this schedule automatically whenever `make up` starts the
# embedded-db profile. Mirrors deploy/helm/hookflow/templates/db-backup-cronjob.yaml
# for the Kubernetes path.
BACKUP_DIR=./backups
BACKUP_RETENTION_DAYS=30
# How often the `db-backup` Compose service runs a backup, in seconds.
# Default: 86400 (daily), matching the Helm CronJob's default cadence.
DB_BACKUP_INTERVAL_SECONDS=86400

# ============================================================================
# PRODUCTION CHECKLIST
# ============================================================================
# Before deploying to production, ensure you have:
#   1. Set APP_ENV=production
#   2. Changed ALL secrets: WEBHOOK_ENCRYPTION_KEY, WEBHOOK_ENCRYPTION_SALT,
#      JWT_SECRET, POSTGRES_PASSWORD, REDIS_PASSWORD
#   3. Set SWAGGER_ENABLED=false
#   4. Set WEBHOOK_ALLOW_PRIVATE_IPS=false
#   5. Set DB_SSL_MODE=require (or verify-full)
#   6. Set HOOKFLOW_BIND=127.0.0.1 and put a reverse proxy in front
#   7. Set EMAIL_ENABLED=true and configure SMTP
#   8. Set CORS_ALLOWED_ORIGINS to your actual domain
#   9. Run: make up-prod (or make up-prod-external)
#  10. Verify with: make health
