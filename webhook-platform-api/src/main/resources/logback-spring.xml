<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <springProfile name="!production">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] [%X{correlationId:-}] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.webhook.platform" level="DEBUG"/>
    </springProfile>

    <springProfile name="production">
        <appender name="CONSOLE_JSON" class="ch.qos.logback.core.ConsoleAppender">
            <!-- P3-36b: no <includeMdcKeyName> here — that element switches
                 LogstashEncoder from "include the whole MDC map" to an allow-list,
                 which was silently dropping organizationId/userId/projectId (set by
                 JwtAuthenticationFilter/ApiKeyAuthenticationFilter) from every
                 production log line even though correlationId made it through. With
                 no include/exclude configured, the encoder emits the entire MDC
                 context map, which is what Loki needs to pivot on
                 correlationId/organizationId per docs/runbooks/trace-webhook-logs.md. -->
            <encoder class="net.logstash.logback.encoder.LogstashEncoder"/>
        </appender>
        <root level="INFO">
            <appender-ref ref="CONSOLE_JSON"/>
        </root>
        <logger name="com.webhook.platform" level="INFO"/>
    </springProfile>
</configuration>
